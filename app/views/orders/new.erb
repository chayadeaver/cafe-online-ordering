<h2>Cafe Menu</h2>
<div>
<h3><u>Coffee</u></h3>
<br>
<form action="/orders" method="POST">
<ul>
<% @items.each do |item| %>
    <%if item.category.name == "coffee" %>
        <li><input type="checkbox" name="order[item][title]" id="<%= item.id %>" value="<%= item.title %>"><%= item.title %> - <%= item.price %></li>
        <br>
    <% end %>

<% end %>
</ul>
</div>

<div>
<h3><u>Tea</u></h3>
<br>
<ul>
<% @items.each do |item| %>
    <%if item.category.name == "tea" %>
        <li><input type="checkbox" name="order[item][title]" id="<%= item.id %>" value="<%= item.title%>"><%= item.title %> - $<%= item.price%></li>
        <br>
    <% end %>

<% end %>
</ul>
</div>

<div>
<h3><u>Food</u></h3>
<br>
<ul>
<% @items.each do |item| %>
    <%if item.category.name == "food" %>
        <li><input type="checkbox" name="order[item][title]" id="<%= item.id %>" value="<%= item.title %>"><%= item.title %> - $<%= item.price%></li>
        <br>
    <% end %>

<% end %>
</ul>
<input type="submit" id="submit" value="Add to Order">
</form>
</div>
